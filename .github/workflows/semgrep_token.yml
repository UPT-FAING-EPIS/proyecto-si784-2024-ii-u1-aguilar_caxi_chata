name: Semgrep Analysis

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  semgrep:
    name: Semgrep Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install Semgrep
        run: |
          python -m pip install --upgrade pip
          pip install semgrep

      # Configurar token de Semgrep como variable de entorno
      - name: Run Semgrep analysis
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_TOKEN }}  # Establece el token como variable de entorno
        run: semgrep --config=auto --error
